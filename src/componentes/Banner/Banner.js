import './Banner.css'

function Banner (){
    return(
        // Isso é o chamado JSX --> É como o react trabalha com a parte visual
        <header className="banner">
            <img src="/imagens/banner.png" alt="O banner principal da página do organo" />
            
        </header>
    )
}

export default Banner